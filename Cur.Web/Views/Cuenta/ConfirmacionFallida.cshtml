@{
    ViewData["Title"] = "No pudimos confirmar";
}

<div class="cv-auth-card text-center">
    <h1>El enlace no es válido</h1>
    <p class="text-muted mb-4">
        Puede que ya lo hayas usado o que haya caducado. Inicia sesión con normalidad;
        si necesitas confirmar el correo, vuelve a solicitarlo desde tu panel.
    </p>
    <a class="btn btn-primary" asp-action="Login">Iniciar sesión</a>
</div>
